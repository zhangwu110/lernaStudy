{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "lint": "eslint --ext .js packages/**/*.js --no-error-on-unmatched-pattern --fix"
  },
  "devDependencies": {
    "eslint": "^8.46.0",
    "eslint-plugin-prettier": "^5.0.0",
    "jest": "^29.6.2",
    "lerna": "^4.0.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "yargs": "^17.7.2"
  }
}
